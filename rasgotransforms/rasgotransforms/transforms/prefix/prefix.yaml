name: prefix
tags:
  - row
  - data_cleaning
description: Add a prefix to each column name
arguments:
  prefix:
    type: value
    description: text to prefix each column name with
example_code: |
  ds = rasgo.get.dataset(74)

  ds2 = ds.prefix(prefix='PRODUCT')

  ds2.preview()