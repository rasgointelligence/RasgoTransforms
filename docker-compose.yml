version: '3.4'

services:
  upload:
    build:
      context: ./
      target: rasgoudt
    working_dir: /var/lib/rasgo/mount/rasgoudt
    command: ./bin/publish-pypi.sh pypi
    environment:
      PYPI_USER: ${PYPI_USER}
      PYPI_PASSWORD: ${PYPI_PASSWORD}
    volumes:
      - ./:/var/lib/rasgo/mount/