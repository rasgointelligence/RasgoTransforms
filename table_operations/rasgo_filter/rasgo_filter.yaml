name: rasgo_filter
transform_type: table
source_code: rasgo_filter.sql
description: Filter a table based on a value
arguments:
  filter_col:
    type: column_list
    description: dimension columns after the pivot runs
  filter_value:
    type: column
    description: column to pivot and aggregate
example_code: |
    stock_source = rasgo.get.data_source(id=1151)
    
